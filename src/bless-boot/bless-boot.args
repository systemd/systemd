# SPDX-License-Identifier: LGPL-2.1-or-later

option -h --help
help Show this help
        return help();

option --version
help Print version
        return version();

option --path=PATH
help Path to the $BOOT partition (may be used multiple times)
        r = strv_extend(&arg_path, optarg);
        if (r < 0)
                return log_oom();
