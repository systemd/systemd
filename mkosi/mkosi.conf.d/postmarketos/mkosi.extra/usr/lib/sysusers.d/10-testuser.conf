# postmarketOS does not support userdbd, so creating the test user via
# userdbd credentials does not work. We need to create the user entries
# in /etc/passwd and /etc/group using sysusers instead.
u testuser 4711 testuser /home/testuser /bin/bash
