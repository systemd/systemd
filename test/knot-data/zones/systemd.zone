; SPDX-License-Identifier: LGPL-2.1-or-later
$TTL 86400
$ORIGIN systemd.

@ IN SOA primary.dns.systemd. root.dns.systemd. (
    42   ; serial
    3H   ; refresh
    15M  ; retry
    1W   ; expire
    1D   ; minimum TTL
)

; NS info
@                     NS primary.dns.systemd.
primary.dns           A  1.2.3.1

dns                   NS primary.dns.systemd.
unsigned              NS primary.dns.systemd.
